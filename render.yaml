# Render.com deployment configuration
services:
  - type: web
    name: va-polling-validator
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd webapp/backend && python app.py
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: "3.11"
